<Window x:Class="VlessVpnClient.App.Views.ImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Импорт VLESS / подписки"
        Width="640" Height="480"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        FontFamily="Segoe UI"
        FontSize="13"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Тип источника:" FontWeight="SemiBold" Margin="0,0,0,8" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <RadioButton x:Name="ModeVless" Content="vless:// URL" IsChecked="True" Margin="0,0,16,0" />
            <RadioButton x:Name="ModeSubUrl" Content="URL подписки" Margin="0,0,16,0" />
            <RadioButton x:Name="ModeSubText" Content="Содержимое подписки" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    Visibility="{Binding ElementName=ModeSubUrl, Path=IsChecked, Converter={StaticResource BoolToVisibility}}"
                    Margin="0,0,0,8">
            <TextBlock Text="Имя подписки:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox x:Name="SubscriptionName" Width="280" />
        </StackPanel>

        <TextBox x:Name="InputBox"
                 Grid.Row="3"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Cascadia Code, Courier New"
                 FontSize="12" />

        <TextBlock x:Name="StatusText"
                   Grid.Row="4"
                   Foreground="{DynamicResource MutedForegroundBrush}"
                   Margin="0,12,0,0"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Отмена" Style="{StaticResource SecondaryButton}" Click="Cancel_Click" Width="100" Margin="0,0,8,0" />
            <Button Content="Импорт" Style="{StaticResource PrimaryButton}" Click="Ok_Click" Width="120" />
        </StackPanel>
    </Grid>
</Window>
